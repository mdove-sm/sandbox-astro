---
import type { ServicePageContentResponseModel } from "@/types";
// import BlocklistLoop from "../layouts/BlocklistLoop.astro";

interface Props {
	model: ServicePageContentResponseModel;
}

const { model } = Astro.props;
---
<h1>{model.name} Page</h1>
<h2>{model.contentType} Template</h2>
<!-- {model.properties?.modules?.items?.length && <BlocklistLoop blocks={model.properties.modules} />} -->
