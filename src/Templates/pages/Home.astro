---
import type { HomeContentResponseModel } from "@/types";
import BlocklistLoop from "../layouts/BlocklistLoop.astro";

interface Props {
	model: HomeContentResponseModel;
}

const { model } = Astro.props;
---
<h1>{model.name} Page</h1>
<h2>{model.contentType} Template</h2>
<!-- {model.properties?.modules?.items?.length && <BlocklistLoop blockList={model.properties.modules} />} -->
